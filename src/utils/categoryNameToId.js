export const categoryNameToId = (name) => {
    switch (name) {
        case "estilo-de-vida":
          return 1;
        case "impacto-ambiental":
          return 2;
        default:
          return 0;
      }
    return
  };